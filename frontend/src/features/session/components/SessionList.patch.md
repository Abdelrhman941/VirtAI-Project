# `SessionList.tsx` — one-line patch

Only line 250 changes. Keep the rest of the file exactly as it is.

```diff
- <div className="sidebar-inner" style={{ width: '100%', position: 'relative' }}>
+ <div className="sidebar-inner w-full relative">
```

No other edits are needed in this file. The `sidebar-inner` class hook is
preserved for any CSS still targeting it.
